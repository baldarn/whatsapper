{
  "name": "whatsapper",
  "version": "1.0.1",
  "description": "Web api for whatsapp-web.js",
  "main": "server.js",
  "keywords": [],
  "author": "baldarn",
  "license": "MIT",
  "dependencies": {
    "@fastify/view": "^12.0.0",
    "ejs": "^3.1.9",
    "fastify": "^4.25.2",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.7"
  },
  "devDependencies": {
    "tap": "^16.1.0"
  },
  "scripts": {
    "start": "node app/server.js",
    "setup-buildx": "docker run --privileged --rm tonistiigi/binfmt --install all && docker buildx create --name multi --driver docker-container --use",
    "push": "docker buildx build --push --platform linux/amd64,linux/arm64 --tag ghcr.io/baldarn/whatsapper:$TAG --tag ghcr.io/baldarn/whatsapper:latest ."
  }
}
