version: '3.9'

services:
  whatsapper:
    image: baldarn/whatsapper
    build:
      context: .
    container_name: whatsapper
    volumes:
      - ./app:/app
      - whatsapper:/wwebjs
    ports:
      - 4000:3000

volumes:
  whatsapper:
